# Restricter Textarea

An text area with character count for [roadmat.sh](https://roadmap.sh/projects/restricted-textarea).

## How to Run

Open `index.html` in your browser, or start a local server:

```bash
python3 -m http.server 8000
```

Then open:

```text
http://localhost:8000
```